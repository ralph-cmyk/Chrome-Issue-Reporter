{
  "name": "chrome-issue-reporter",
  "version": "21.0.1",
  "description": "Chrome extension that captures page context and helps file GitHub issues directly from your browser",
  "scripts": {
    "clean": "rm -rf dist chrome-issue-reporter*.zip",
    "build": "npm run clean && mkdir -p dist && cp -r extension/* dist/ && node scripts/apply-update-url.js",
    "package": "npm run build && VERSION=$(node -p \"require('./extension/manifest.json').version\") && cd dist && zip -r ../chrome-issue-reporter-v${VERSION}.zip * && cd .. && echo \"\\n✅ Extension packaged successfully: chrome-issue-reporter-v${VERSION}.zip\"",
    "format": "npx -y prettier@3.3.3 --write \"extension/**/*.{js,html,json}\" \"scripts/**/*.js\" \".github/workflows/*.yml\"",
    "format:check": "npx -y prettier@3.3.3 --check \"extension/**/*.{js,html,json}\" \"scripts/**/*.js\" \".github/workflows/*.yml\"",
    "build-update": "node scripts/build-update.js",
    "generate-update-xml": "node scripts/update-manifest.js",
    "setup-cloudflare": "node scripts/setup-cloudflare.js",
    "release:r2": "bash scripts/release-r2.sh"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/ralph-cmyk/Chrome-Issue-Reporter"
  },
  "keywords": [
    "chrome",
    "extension",
    "github",
    "issue-reporter",
    "manifest-v3",
    "bug-reporting",
    "developer-tools",
    "chrome-web-store"
  ],
  "author": "Chrome Issue Reporter Contributors",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/ralph-cmyk/Chrome-Issue-Reporter/issues"
  },
  "homepage": "https://github.com/ralph-cmyk/Chrome-Issue-Reporter#readme"
}
