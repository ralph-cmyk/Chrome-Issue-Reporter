{
  "name": "chrome-issue-reporter",
  "version": "0.1.0",
  "description": "Chrome extension that captures page context and helps file GitHub issues",
  "scripts": {
    "clean": "rm -rf dist chrome-issue-reporter.zip",
    "build": "npm run clean && mkdir -p dist && cp -r extension/* dist/",
    "package": "npm run build && cd dist && zip -r ../chrome-issue-reporter.zip * && cd .. && echo '\n✅ Extension packaged successfully: chrome-issue-reporter.zip'",
    "prerelease": "npm run package"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/ralph-cmyk/Chrome-Issue-Reporter"
  },
  "keywords": [
    "chrome",
    "extension",
    "github",
    "issue-reporter",
    "manifest-v3"
  ],
  "author": "",
  "license": "MIT"
}
